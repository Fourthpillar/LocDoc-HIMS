description = 'LockDoc doctor module - doctor profile, facility mappings, availability/schedules, consultation records, prescriptions, doctor reports'

dependencies {
    api project(':outpatient')
}
